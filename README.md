# musictrack-hummingbird

```
chmod 777 resources/db
//or chown www-data:www-data resources/db or similar

sudo php -S 127.0.0.1:80 index.php
//then will be running on localhost port 80, modify port if needed
```
